{
  "private": true,
  "scripts": {
    "sitemap": "node scripts/generate-sitemap.js",
    "gemeenten": "node scripts/generate-gemeenten.js",
    "nav:sync": "node scripts/sync-nav-visibility.js",
    "seo:build": "node scripts/generate-gemeenten.js && node scripts/sync-nav-visibility.js && node scripts/generate-sitemap.js"
  },
  "dependencies": {
    "@upstash/redis": "^1.38.0",
    "nodemailer": "^9.0.0"
  }
}
